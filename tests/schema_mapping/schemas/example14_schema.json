{
    "definitions": {
    },
    "example": {
        "type": "object",
        "properties": {
            "subject": {"enum": ["foo"]},
            "other_subject": {"enum":  ["bar"]},
            "name": {"type": "string"},
            "i": {"type":  "integer", "default":  5},
            "other": {"enum":  ["example"]}
        },
        "required": ["i", "name", "subject", "other_subject", "other"],
        "additionalProperties": true
    }
}
